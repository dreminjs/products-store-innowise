import { FC } from "react";

interface IFilterHeaderProps {
  label: string;
}

export const FilterHeader: FC<IFilterHeaderProps> = ({ label }) => {
  return (
    <div>
      <button></button>
    </div>
  );
};
