import { FC } from "react";
import styles from "./FilterSelection.module.css";
interface IFilterSelectionProps {}

export const FilterSelection: FC<IFilterSelectionProps> = ({}) => {
  return <div className={styles.FilterSelection}></div>;
};
